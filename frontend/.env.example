# where the browser sends api calls. empty means same-origin: the client calls
# /api/... and the web host rewrites that to the api service, which keeps the
# session cookie first-party. set an absolute url only when the api is served
# from its own domain, and then set COOKIE_SAME_SITE=none on the api.
VITE_API_URL=
